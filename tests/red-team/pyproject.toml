[project]
name = "microsoft-foundry-red-team-tests"
version = "0.1.0"
description = "Local red-team test harness for this repository"
requires-python = ">=3.10,<3.14"
dependencies = [
  "azure-identity==1.25.3",
  "pyrit==0.14.0",
]

[tool.uv]
package = false
